// Comando: `axionos generate:edge-function --name agent-analyzer --stage 15`
    // Gera em `/supabase/functions/agent-analyzer/`:
    // 1. index.ts com template padrão (handler, logging, client Supabase)
    // 2. Esqueleto do Agent Contract
    // 3. Configuração de tempo limite e recursos
    // 4. Teste de unidade básico